.page-header
  %h= 'Edit article'
%form{:name => 'edit_form', :ole => 'form', :method => 'post', :action => './post', :onSubmit => "return check_form_dual('title', 'entry')"}
  .form-group
    %label= 'Title'
    %input{:name => 'title', :type => 'text', :class => 'form-control', :placeholder => 'title', :value => "#{@title}"}
  .form-group
    %label= 'Entry'
    %textarea{:name => 'entry', :class => 'form-control', :rows => '20'}= @body
    .btn-group
      %button{:type => 'button', :class => 'btn btn-default', :onClick => "add_str_at_cursor('entry', '[read_more]');"}= '[read_more]'
  - if @entryEdit == 'active' then
    .form-group
      %label= 'Category'
    - @category.each do |c|
      .checkbox
        %label
          - if @entryCategory.size > 0 && @entryCategory[0].to_i == c.id then
            - @checked = ''
            - @entryCategory.delete_at(0)
          %input{:type => 'checkbox', :name => 'category[]', :value => c.id, :checked => @checked}= c.name
  .form-group{:style => 'margin-top:30px;'}
    %button{:type => 'submit', :name => 'submit', :value => 'post', :class => 'btn btn-default'}= 'Post'
    %button{:type => 'submit', :name => 'submit', :value => 'save', :class => 'btn btn-default'}= 'Save'
    %button{:type => 'submit', :name => 'submit', :value => 'delete', :class => 'btn btn-default', :style => 'float: right;'}= 'Delete'
