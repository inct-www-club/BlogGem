    <div class="page-header">
        <h1><%= @article %></h1>
    </div>
    <% @element.each do |e| %>
    <div class="panel panel-default">
        <div class="panel-heading"><%= e.title %></div>
        <div class="panel-body">
            <%= e.body %>
        </div>
    </div>
    <% end %>
